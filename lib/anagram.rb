class Anagram
  
attr_accessor :word

def initialize(word)
  @word = word
end

def anagram_detector(array)
  
    if %w(array).split == %w(array).sort
      return word
    else
      nil
end
end
end
